function [ output_args ] = brain_analysis( svs_path, analysis_output_path )

    global TRAIN_PATH TEST_PATH  RESULTS_PATH; 
    
    if (svs_path(end) ~= '/')
       svs_path =  
    end

    TEST_PATH    = svs_path;
    RESULTS_PATH = analysis_output_path;
    
    

end

